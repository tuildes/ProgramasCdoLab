#include <stdio.h>

int main () {

	// Declaracao de variaveis

		// Comentario sobre o uso da variavel
	
	// Parte 1

		// Comentario que explica parte do funcionamento
		// Linhas em branco
		// Usuario precisa saber o que digitar
	
	if (1 == 2) {
		return 1;
	}; // nao precisa, para for, while, if...

	if (1 == 2) 
		return 1;
		// melhor
	
	if ((a==b) && (b==c) {
		// eh bom colocar parenteses para garantir
	};

	// compilar com nome + -Wall, - Werrror - transforma warning em error
	// Manual do codigo

	// Parte 2

		// Nao entregar ./exec

	return 0;
}
